
const prompt = require("prompt-sync")();

let userInput = prompt("Enter temperature in celsius:");

function converToFahrenheit(temp){

Faherenheit =  temp * (9/5) + 32

	console.log(Faherenheit);
}

converToFahrenheit(userInput)